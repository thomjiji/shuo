<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="WindowsDictation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WindowsDictation"
    xmlns:automation="using:Microsoft.UI.Xaml.Automation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Windows Dictation"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="Windows Dictation">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <ScrollViewer Grid.Row="1">
            <StackPanel
                MaxWidth="680"
                Margin="0,0,0,32"
                Padding="32"
                Spacing="20">
                <StackPanel Spacing="6">
                    <TextBlock
                        FontSize="32"
                        FontWeight="SemiBold"
                        Text="Windows Dictation" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        FontSize="16"
                        Text="本地模型、全局热键、自动粘贴。" />
                </StackPanel>

                <InfoBar
                    x:Name="StatusInfo"
                    IsOpen="True"
                    IsClosable="False"
                    Severity="Informational"
                    Title="正在启动"
                    Message="正在连接本地听写服务。" />

                <Border
                    Padding="24"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="16">
                    <Grid ColumnSpacing="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border
                            Width="52"
                            Height="52"
                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                            CornerRadius="16">
                            <FontIcon
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="24"
                                Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                Glyph="&#xE720;" />
                        </Border>
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock
                                x:Name="StatusTitle"
                                automation:AutomationProperties.AutomationId="StatusTitle"
                                FontSize="20"
                                FontWeight="SemiBold"
                                Text="正在启动" />
                            <TextBlock
                                x:Name="StatusDetail"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                TextWrapping="Wrap"
                                Text="正在加载本地听写服务。" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border
                    Padding="20"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    CornerRadius="16">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="4">
                            <TextBlock FontWeight="SemiBold" Text="全局快捷键" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="按一次开始录音，再按一次停止并转写。" />
                        </StackPanel>
                        <Border
                            Grid.Column="1"
                            Padding="12,7"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <TextBlock
                                FontFamily="Cascadia Mono"
                                FontWeight="SemiBold"
                                Text="Ctrl + Alt + \" />
                        </Border>
                    </Grid>
                </Border>

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button
                        x:Name="ToggleButton"
                        MinWidth="136"
                        Click="ToggleButton_Click"
                        Content="开始录音"
                        Style="{StaticResource AccentButtonStyle}" />
                    <Button Click="ExitButton_Click" Content="退出" />
                </StackPanel>

                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap"
                    Text="录音和转写状态会出现在当前屏幕下方中央，不会抢走正在输入的窗口。" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
